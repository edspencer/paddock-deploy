---
# Ansible collections this role uses. Install with:
#   ansible-galaxy collection install -r requirements.yml
collections:
  - name: community.general   # npm module
    version: ">=8.0.0"
  - name: community.docker    # docker_image (docker deploy method)
    version: ">=3.0.0"
