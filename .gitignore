# Local env / secrets — never commit
.env
*.env
!*.env.example

# Terraform / OpenTofu state + local var files
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl
*.tfvars
!*.tfvars.example

# Ansible local inventory (keep the .example)
hosts.ini

# Kubernetes secrets rendered from *.example.yaml — never commit
kubernetes/secret.yaml

# Editor / OS cruft
node_modules/
.DS_Store
