# Local env / secrets — never commit
.env
*.env
!*.env.example

# Terraform / OpenTofu state
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl

# Kubernetes secrets rendered from *.example.yaml — never commit
kubernetes/secret.yaml

# Editor / OS cruft
node_modules/
.DS_Store
