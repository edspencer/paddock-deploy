# Example htpasswd file — DO NOT use in production; generate your own with
# ./gen-htpasswd.sh (which writes ./htpasswd, gitignored).
#
# Format: one "username:hash" per line. The hash below is an apr1 hash of the
# password "changeme" for the user "admin". Replace it — never ship this.
admin:$apr1$Zt1eJ8mS$3o5r7t9v1x0uJ8mS1p3o51
